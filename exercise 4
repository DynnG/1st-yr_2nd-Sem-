/*Name: Geraldyn Cataluna                                       BSCS 1A
This Java program defines a class HelloObject with two constructors and a method to print a message stored in its instance variable message. The first constructor initializes the message with a provided string value, while the second constructor initializes it with the same value as another HelloObject object. The HelloTester class tests the functionality by creating instances of HelloObject, setting and retrieving messages, demonstrating the use of both constructors.
*/
class HelloObject {
    private String message; // Instance variable to store the greeting

    // Constructor for HelloObject
    public HelloObject(String message) {
        this.message = message; // Initialize message with the provided value
    }

    // Additional constructor to initialize greeting from another HelloObject object
    public HelloObject(HelloObject init) {
        this.message = new String(init.message); // Create a new String object with the same value
    }

    // Method to print the greeting
    public void speak() {
        System.out.println(message);
    }
}

public class HelloTester {
    public static void main(String[] args) {
        HelloObject anObject = new HelloObject("Hello from an object!");
        anObject.speak(); // Output: "Hello from an object!"

        // Create a new HelloObject using the additional constructor with an existing HelloObject
        HelloObject anotherObject = new HelloObject(anObject);
        anotherObject.speak(); // Output: "Hello from an object!"
    }
}
